"""Calibration routines placeholder."""

from __future__ import annotations


def pixel_to_mm(pixels: float, scale: float) -> float:
    """Convert pixels to millimeters.

    Parameters
    ----------
    pixels : float
        Number of pixels.
    scale : float
        Pixels per millimeter.
    """
    return pixels / scale
